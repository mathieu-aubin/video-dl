language: ruby
rvm:
 - 2.1

sudo: required
addons:
  apt:
    packages:
    - build-essential
    - grep
    - sed

script: ". ./travis/cibuild"

before_install:
- openssl aes-256-cbc -K $encrypted_a7a9028aa283_key -iv $encrypted_a7a9028aa283_iv
  -in s.key.enc -out s.key -d
