language: ruby
rvm:
- 2.1

sudo: required
addons:
  apt:
    packages:
    - build-essential
    - grep
    - sed

script: ". ./travis/cibuild"

before_install:
- openssl aes-256-cbc -K $encrypted_af135e3dfc23_key -iv $encrypted_af135e3dfc23_iv
  -in s.key.enc -out s.key -d
